function setup() {
  createCanvas(600, 600);
  textAlign(CENTER, CENTER);
  textSize(80);
  fill(0);
  stroke(0);
  strokeWeight(1);
  background(255);
	fill(0);
	textSize(53);
	text("Click to play", 250, 300);
	
}

function mouseClicked() {
  background(255);
  var num1 = int(random(1, 1000));
  var num2 = int(random(1, 1000));
  var output = num1 + " + " + num2 + " = ";
	var input =   + (num1 + num2)
  text(output, width / 2, height / 2);
}
